const Discord = require ("discord.js");

module.exports = {
   run: async (client, botUtils, message, args) => {
     / **
      * Placeholder for the canvas module
      *
      * DO NOT DELETE
      * /
     return;
   },

   config: {
     name: "errorcanvas",
     aliases: ["errc", "ec"],
     description: "Check the errors saved by the error handler",
     usage: "error",
     accessableby: "Developers"
   }
}